let initialState = {
    friends: [
        { id: 1, name: 'Andreea' },
        { id: 2, name: 'Ciburecik' },
        { id: 3, name: 'Figucik' }
    ]
}

const sidebarReducer = (state = initialState, action) => {
    return state
}

export default sidebarReducer